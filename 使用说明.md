# MindVault 本地知识库

## 快速开始

### 方式一：双击启动（推荐）
1. 双击 `启动 MindVault.bat` 文件
2. 等待几秒钟，浏览器会自动打开
3. 开始使用！

### 方式二：命令行启动
```bash
cd .next\standalone
node server.js
```

## 首次使用

1. 访问 [http://localhost:3000](http://localhost:3000)
2. 点击「导入」页面
3. 从 Claude.ai 导出你的对话数据
4. 上传 conversations.json 文件

## 数据存储

- 数据库位置：`data/dev.db`（SQLite 格式）
- 所有数据完全本地存储，不会上传到任何服务器

## 常见问题

### 端口被占用
如果提示端口 3000 被占用，可以修改启动脚本中的 `PORT` 变量

### 浏览器未自动打开
手动访问 http://localhost:3000

### 停止服务器
在命令行窗口按 `Ctrl+C`

## 技术信息

- 框架：Next.js 14.2.15
- 数据库：SQLite (Prisma ORM)
- 运行时：Node.js 18+
- 端口：3000
